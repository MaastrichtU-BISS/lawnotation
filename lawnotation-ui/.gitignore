node_modules
*.log*
.nuxt
.nitro
.cache
.output
.env
.env.prod
.env.local
dist
.DS_Store
.vercel

# auto-regenerated by pnpm 11+ locally; Vercel still builds with pnpm 9.x,
# which errors on this file (requires a "packages" field). See package.json#pnpm
# for the equivalent overrides/onlyBuiltDependencies config that Vercel reads.
pnpm-workspace.yaml

cypress/downloads
cypress/screenshots
cypress/videos
test-results.xml
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
playwright/.auth
